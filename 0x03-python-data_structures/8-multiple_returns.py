#!/usr/bin/python3
def multiple_returns(sentence):
    lon = len(sentence)
    first_char = sentence[0]

    if lon == 0:
        sentence[0] is None
    else:
        _tuple = (lon, first_char)
        return _tuple
